;;;; free-vars.lisp - Free variable analysis

(in-package #:clysm/compiler/analyzer/free-vars)

(defun collect-free-variables (ast env)
  "Collect free variables in an AST node."
  ;; TODO: Implement
  (declare (ignore ast env))
  nil)
