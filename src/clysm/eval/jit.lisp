;;;; jit.lisp - Tier 2 JIT compilation

(in-package #:clysm/eval/jit)

(defun jit-compile (expr)
  "JIT compile an expression to Wasm and instantiate."
  ;; TODO: Implement Tier 2 JIT
  (declare (ignore expr))
  nil)
