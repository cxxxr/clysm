;;;; interpreter.lisp - Tier 1 S-expression interpreter

(in-package #:clysm/eval/interpreter)

(defun interpret (expr &optional env)
  "Interpret an S-expression."
  ;; TODO: Implement Tier 1 interpreter
  (declare (ignore expr env))
  nil)
