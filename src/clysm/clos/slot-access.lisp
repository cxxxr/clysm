;;;; slot-access.lisp - Slot access

(in-package #:clysm/clos/slot-access)

(defun slot-value* (instance slot-name)
  "Get slot value."
  ;; TODO: Implement
  (declare (ignore instance slot-name))
  nil)

(defun set-slot-value* (instance slot-name value)
  "Set slot value."
  ;; TODO: Implement
  (declare (ignore instance slot-name value))
  nil)
