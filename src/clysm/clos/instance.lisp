;;;; instance.lisp - Instance creation

(in-package #:clysm/clos/instance)

(defun make-instance* (class &rest initargs)
  "Create an instance of a class."
  ;; TODO: Implement
  (declare (ignore class initargs))
  nil)
